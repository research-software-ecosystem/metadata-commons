*.idea
venv
.DS_Store

# In light of dataset dumps being stored on GHCR,
# any future dataset dumps at the usual place should be ignored.
datasets/

# Ignore the Debian import log as it lives in the GH Action (temporaril
debian_import.log

# Ignore cache sqlite files.
*.sqlite
